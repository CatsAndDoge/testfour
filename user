createUser(string _userName) public returns (uint)
	创建用户，输入用户名，返回排序号

inquireUserA(address _userAddress) public returns (string , address , uint)
	查询用户，输入用户地址，返回用户信息

inquireUserN(string _userName) public returns (string , address , uint)
	查询用户，输入用户名，返回用户信息

changeUserName(string _name) public returns (bool) 
    修改用户姓名,成功返回true,失败返回false

deleteUser() public returns (bool)
    删除用户信息，删除成功返回true
