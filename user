createUser(string _userName) returns (uint)
	创建用户，输入用户名，返回排序号

inquireUserA(address _userAddress) returns (string , address , uint)
	查询用户，输入用户地址，返回用户信息

inquireUserN(string _userName) returns (string , address , uint)
	查询用户，输入用户名，返回用户信息
